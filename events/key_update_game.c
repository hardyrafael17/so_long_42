#include "../so_long.h"

int	key_update_game(int key, void *param)
{
  //validate key
  //calc next move
  //call next move
  {
	printf("exit over here\n");
	return (1);
  }
  if(!param)
	handle_error("no_param");

  printf("%d\n", key);
  return (0);
}
